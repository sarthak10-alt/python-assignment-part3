log_error("trigger_errors", "HTTP error", f"{response.status_code} Not found for product ID 999")
print(f"Logged: HTTP{response.status_code} Error")
except exception as e:
log_error("trigger_errors", "Request_errors", str(e))
def display_logs():
print("\n--- current contents of error_log.txt ---")
if os.path.exists(LOG_FILE)
  with open(LOG_FILE, "r", encoding="utf-8") as f:
content = f.read()
  print(content if content else "Log File is empty.")
else:
print("Log File not found.")
if __name__ == "__main__":
trigger_errors()
display_logs()
